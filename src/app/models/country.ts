export class ResCountry {
    static model = 'res.country';
    static fields = {
        "name": "text",
        "code": "text"
    };
    static fields_changeable = [
    ];
    static fields_display_mapper = {
    }
}