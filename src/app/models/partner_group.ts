export class PartnerGroup {
    static model = 'partner.group';
    static fields = {
        "name": "text",
    };
    static fields_changeable = [
    ];
    static fields_display_mapper = {
    }
}